# تقرير تحقّق: كل النقاط مقابل الدروس + النقاط الزائدة — v1.70

**الملف:** `M-Waves_M1.pine` — «v1.70». **التاريخ:** 2026-06-11.  
**المنهج:** تحقّق فعليّ من الكود (بالأسطر) مقابل نصّ الدرس — تأكيد/تصحيح — ثم **النقاط الزائدة** (ما يضيفه الكود فوق الدرس).

---

# الجزء أ — التحقّق بنداً بنداً (تأكيد/تصحيح)

## L1‑05 الصغيرة — ✅ مؤكَّد
- الشمعتان الصحيحتان (جسم ≥ كل ذيل) + تجاهل الابتلاع + التتابُع/الفعالية. مطابق.
- `smallActiveDir` بانقلاب البنية. مطابق. **95% — لا تصحيح.**

## L1‑06 المتوسطة — ✅ مؤكَّد
- اشتقاق بالكسر؛ الخط على الجسم (`levelM`)؛ الفعالية **التزايُدية** (تغطية 1088‑1092 + تتابُع 1098‑1108). مطابق. **95% — لا تصحيح.**

## L1‑07 الزرقاء — ✅ مؤكَّد (مع تحفّظ افتراضيّ)
- `kP/kB` + آخر 7 + التنظيف. مطابق. `useBigBreak=false` ⇒ الافتراضي legs. **85% — لا تصحيح.**

## L1‑08 السيولة + المستويات — ⚠️ **تصحيح**
**مؤكَّد (السطر 270‑286):**
- السيولة: صعوداً = آخر قاعين (`lbP,pbP`)؛ هبوطاً = آخر قمتين (`ltP,ptP`). ✅
- بوّابة الموجة النشطة: تُرسم فقط إن كانت **النقطتان ضمن `waveStart`** (271/276) ⇒ لا تاريخ التاريخ. ✅
- المستوى = جسم آخر تطرّف (`lbBody/ltBody`). ✅

**🔴 التصحيح:** ادّعى التقرير «منع التكدّس بـclamp ضمن النطاق». **في v1.70 أُزيل القصّ** (السطر 274: «الامتداد يتبع `liqExt` فقط — لا قصّ بالنطاق؛ التكدّس يُحَلّ بـ Scale price chart only»). ⇒ **الإسقاط بالميل للحافة دون قصّ**، والتكدّس يُعالَج بـ**إعداد عرض الشارت** لا بالكود.
- **الأثر:** سليم نصّياً (الخط يمتدّ فعلاً بالميل)، لكنه **يعتمد على ضبط المستخدم** «Scale price chart only»؛ وإلّا قد يعود التكدّس بصرياً.
- **النسبة المصحّحة: 92%** (كان 95%) — لتعليق التكدّس على إعداد خارجيّ.

## L1‑09 التجدّد — ✅ مؤكَّد
- `structuralBreak` (395‑410): صعوداً = **شمعتان حمراوان** (`c<o`) تحت الخط؛ هبوطاً = **شمعتان خضراوان** فوقه ⇒ كسر. مطابق نصّاً. ✅
- `useRenewal=false`، `useSmallTL=false`. **80% — لا تصحيح** (مطفأ افتراضياً).

## L1‑10 الاستنتاجات — ⚠️ **تصحيح (رفع)**
**مؤكَّد (السطر 2059‑2085):** «قوة السيولة» **أدقّ ممّا قُدِّر**:
- تقارن القمة/القاع التصحيحيّ بـ**المستوى المكسور السابق** = **ثاني** قاع/قمة متوسط قبل النقطة الجديدة (تتخطّى الجديدة). قويّة إن بقيت خارجه، ضعيفة إن دخلته. **مطابق تماماً للدرس** («ضعيفة إن دخل القاع نطاق القمة السابقة»). ✅
- + اتجاه الموجة + اتجاه السيولة + الجهة المسموحة. ✅
- **النسبة المصحّحة: 93%** (كان 90%) — «الضعيفة» ليست مبسّطة بل دقيقة.

## L1‑11 المراحل الخمس — ✅ مؤكَّد
- آلة المراحل (288‑378)، الأكواد 0‑4، كسر نصف الجسم، `despDeep`. مطابق. **90% — لا تصحيح.**

## L2‑01 بنية المستوى 2 — ✅ **90%.**

## L2‑02 المناطق — ✅ مؤكَّد
- خوارزمية islast + عضوية + `f_effForZone` + الجوار الفعّال. مطابق. المستطيلات مطفأة + لا سقف عرض. **85% — لا تصحيح.**

## L2‑03 فرص الدخول — ✅ مؤكَّد (v1.70)
- `oppBasic = تجميع/تصريف`، `oppHR`، `pend`، إبطال بقاع الإعداد + بوّابة القرب. مطابق. تكرار الإشارة يحتاج تأكيد؛ الثانوية بلا لون. **78% — لا تصحيح.**

## L2‑04 شروط الانعكاس — ✅ مؤكَّد (v1.70)
- خريطة الدرجات + الوقف من المنطقة (مؤكّد 1330 + «؟» 1402) + شبكة الأمان (1332). الوقف المعكوس **زال**. [E5] غير منفّذ. **82% — لا تصحيح.**

---

# الجزء ب — النقاط الزائدة (فوق الدرس) — مطلوبك «حتى الزيادة»

| # | الإضافة | الموقع | التصنيف | الحكم |
|---|---|---|---|---|
| 1 | **بوّابة قرب الإعداد** `buyNearZone` + إبطال الجري البعيد | 1303‑1308، 1366 | هندسيّ | ✅ مفيد (يمنع التقاط كسر بعيد بانتظار قديم) — **ليس نصّاً في الدرس** |
| 2 | **`buf` (هامش مشتقّ من سعة الصغيرة)** | 1242‑1243 | أتمتة حُكم بصريّ | ✅ يمثّل «بقليل»/«قريب» — الدرس لا يكمّمه |
| 3 | **إبطال بقاع الإعداد** `close < buySetupLow − buf` | 1358 | هندسيّ مؤصَّل | ✅ يمثّل «فشل = كسر قاع الإعداد» |
| 4 | **`not buyOK` ضمن الإبطال** | 1358 | إضافة | ⚠️ **ليس في الدرس**؛ قد يمسح pending عند انقلاب اتجاه مؤقّت — مرشّح للمراجعة |
| 5 | **منع الاحتواء** `f_zoneContain` | 558+ | تنقية | ✅ فوق «لا عبر تصحيح» — يمنع منطقة داخل منطقة |
| 6 | **شبكة أمان الجهة** `sP < eP` / `sP > eP` | 1332/1345 | حارس سلامة | ✅ يمنع إشارة معكوسة — ليس نصّاً لكنه ضروريّ |
| 7 | **القصّ بالنطاق (clamp)** | (أُزيل v1.70) | كان زائداً | ⛔ مُزال — التكدّس صار بإعداد الشارت |
| 8 | **`zoneAutoBox`** | 127 | عرض اختياريّ | موسوم صراحةً «**ليست من الاستراتيجية**» |
| 9 | **`useSmallTL`** (كسر مستوى الصغير يقلب الاتجاه) | 116 | تجريبيّ | «قيد الاختبار»، مطفأ |
| 10 | **hysteresis على `bigDir`** | (اتجاه الكبيرة) | هندسيّ | ✅ يمنع التذبذب قرب التوازن |
| 11 | **عدّادات/تلميحات التشخيص** (`diagTip`, cSm3…) | 1318‑1326، 1380 | تشخيص | أدوات تطوير (تُزال للإنتاج) |
| 12 | **`NO_TARGET_ZONE`** (لا اختراع هدف) | 1348+ | حارس | ✅ مؤصَّل (الدرس: الهدف منطقة، لا اختراع) |
| 13 | **مدخلات معايرة** (`minCorr, bufMult, d4tf, zoneExt…`) | إعدادات | تقريب حُكم | تقريبات لاجتهاد المحلّل (لا أرقام نصّية في الدرس) |
| 14 | **override الزرقاء + `mcIsBlue`/`bluePivotBars`** | 544+، 1856+ | تطبيق مؤصَّل | ✅ يمثّل «الزرقاء قمة صفراء صالحة» (الدرس 02) |

**ملاحظة:** الزيادات 1‑3‑5‑6‑10‑12‑14 **مؤصَّلة/مفيدة**؛ والزيادة 4 (`not buyOK`) **مرشّحة للمراجعة**؛ و8‑9‑11 **اختيارية/تجريبية/تشخيصية** (لا تمسّ الجوهر).

---

# الجزء ج — النسب بعد التحقّق (المصحّحة)

| الدرس | قبل | **بعد التحقّق** | السبب |
|---|---|---|---|
| L1‑05 | 95% | **95%** | — |
| L1‑06 | 95% | **95%** | — |
| L1‑07 | 85% | **85%** | — |
| L1‑08 | 95% | **92%** ⬇️ | التكدّس على إعداد خارجيّ (أُزيل القصّ) |
| L1‑09 | 80% | **80%** | — |
| L1‑10 | 90% | **93%** ⬆️ | «الضعيفة» دقيقة (مستوى مكسور سابق) |
| L1‑11 | 90% | **90%** | — |
| L2‑01 | 90% | **90%** | — |
| L2‑02 | 85% | **85%** | — |
| L2‑03 | 78% | **78%** | — |
| L2‑04 | 82% | **82%** | — |

**المتوسّط بعد التحقّق: ~87.7%** (التصحيحان يكاد يتعادلان).

---

# الخلاصة

1. **تأكيد:** معظم البنود مطابقة (الصغيرة/المتوسطة/الزرقاء/المراحل/الاستنتاجات/المناطق/الإشارات).
2. **تصحيحان:** L1‑08 ⬇️ (التكدّس صار بإعداد «Scale price chart only» لا بالكود) — **نبّه المستخدم لضبطه**؛ L1‑10 ⬆️ («الضعيفة» دقيقة).
3. **النقاط الزائدة (14):** أغلبها مؤصَّل/مفيد؛ **الوحيدة المرشّحة للمراجعة = `not buyOK` في الإبطال** (قد يفسّر بقيّة ندرة الإشارة)؛ والاختيارية (`zoneAutoBox/useSmallTL/التشخيص`) لا تمسّ الجوهر.

> توصية فورية: (أ) نبّه المستخدم لتفعيل «Scale price chart only» (L1‑08)؛ (ب) راجِع `not buyOK` في الإبطال (الزيادة 4) — هو المرشّح الأقوى لتفسير تكرار الإشارة المنخفض.
