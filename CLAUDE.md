# M-Waves (الموجة التاريخية) — Pine Script v6

مؤشّر تداول متعدّد الدرجات (Elliott/Dow/SMC) على TradingView. الهدف النهائيّ: تحويله استراتيجية.
لغة المشروع: العربيّة (وثائق، رسائل commit، أسماء المواصفات).

## الملفّات

- `J-Waves-M1.pine` — **النسخة النشطة** (إعادة البناء من الصفر، v2.4x على فرع `m3/gaps-and-scale`)
- `M-Waves_M1.pine` — النسخة القديمة المستقرّة (v3.9x) — مرجع، لا تطوير عليها
- `libraries/JW_*.pine` — المكتبات المنشورة على TradingView (Foundation/Degrees/Lines/Zones/Conclusions/Elliott)
- `reference/`، `_compare/` — مراجع خارجيّة للمقارنة فقط

## الوثائق الحاكمة — اقرأ قبل أيّ تعديل

كلّها في `docs/build/`:

1. `MWaves_architecture_full.md` — المعمارية الفراكتالية أحادية الاتجاه (ط0→ط4)
2. `MWaves_buildplan.md` — خطة البناء ومراحلها
3. `MWaves_coding_charter.md` — دستور الكود (إلزاميّ)
4. `MWaves_inventory.md` — جرد الحالات والمخرجات
5. `WORK_STATE.md` — **حالة العمل الحاليّة (ابدأ منها كلّ جلسة)**
6. `GAPS_TODO.md` — سجلّ الفجوات المؤجَّلة (تُعالَج بطلب المستخدم فقط)
7. `spec_*.md` — مواصفة لكلّ درس/طبقة؛ **المواصفة مصدر الحقيقة**، والدروس (`docs/M-Waves_منطق_الدروس.md`) المرجع الأعلى

## الخطوط الحمراء

- **لا look-ahead**: أيّ قراءة تاريخيّة تُقيَّد بـ`< atBar`؛ قرار الرسم ≠ قرار المنطق
- **حدّ الموجة بنيويّ لا زمنيّ** (تطرّف→تطرّف على الذيول)؛ لا أرقام أيّام ملفَّقة
- **عمِّم على فهرس الدرجة**: سلّم الدرجات مفتوح (صغيرة→متوسطة→كبيرة→أكبر…) — لا كود مثبَّت على 3 درجات
- **لا تلفيق**: عند نقص البيانات المخرَج «غير محدَّد»، لا تخمين
- الفجوات المكتشفة تُسجَّل في `GAPS_TODO.md` ولا تُعالَج إلا بطلب صريح

## المنهجيّة — القياس يغلب البرهان

- كلّ فرضيّة تُقاس على الشارت قبل اعتمادها (عبر TradingView MCP: لقطات + قراءة رسومات + A/B)
- **قاعدة اختبار الطبقات**: عند تعديل أيّ طبقة اختبر كلّ الطبقات عاديًّا والطبقة المستهلِكة (فوقها) بدقّة؛ المعيار = هل المخرَج ينحطّ (شرط دائم الصدق / فرع لا يُبلَغ / قيمة لا تتنوّع)
- **مسح التحقّق**: تنقّل بين الفريمات (كبيرة/صغيرة) والأدوات (مؤشّر/معدن/كريبتو/فوركس) وموجات صاعدة وهابطة — لا لقطة واحدة
- الحَكَم النهائيّ للدرجات = اختبار الدرجات الثلاث

## فخاخ Pine v6

- **«كائن UDT محلّيّ + نداء method» في المكتبات يُترجَم معطوبًا** (كود صحيح، سلوك منهار) — تحذير CE10237 إنذار مبكّر، لا تُسكته
- بروتوكول النشر الآمن: من القرص + مراقبة كونسول الحفظ + A/B فوريّ بعد النشر
- `pine_check` لا يتحقّق من أرقام إصدارات المكتبات؛ `pine_get_source` قد تكون بائتة

## أسلوب العمل

- طبقةً طبقةً (ط0→ط4): إصلاح/تحسين → تقييم الاستخراج مكتبةً → التالي
- رسائل commit عربيّة بنمط: `vX.Y.Z مN-K: وصف موجز` (انظر git log)
- المهارات: فعِّل `mwaves-rebuild` (الدستور) و`mwaves-engineer` في كلّ عمل على المؤشّر؛ `pine-developer/debugger/backtester` حسب الحاجة
- تحقّق من الأثر عند كلّ تعديل: grep المستهلكين + المدخلات اليدويّة + القرارات السابقة
