أنت تعمل على مؤشر Pine Script v6 في:
D:\Projects\MA-Waves\MA-Waves\M-Waves_M1.pine  (العنوان الحالي «M-Waves Engine v1.25»).
هذا هو المصدر الوحيد المعتمد. احفظ التعديل هنا حصراً وارفع الإصدار إلى v1.26.

# المهمة (الدفعة 8 — محصورة جداً)
تفعيل [R2] «تجدّد خط السيولة بكسر الشمعتين» افتراضياً، بعد أن عُولِج تكدّس الشموع في v1.25.
لا تلمس أي شيء آخر.

# قبل التعديل (إلزامي)
1) اقرأ: الدرس 09 (تفريغ «09.تجدد القمم والقيعان.txt») قاعدة [R2] + كتاب الشرح [320‑357] + تقرير «تقرير_الدرسين_08_09_v1.25.md».
2) افحص في الكود: المدخل `useRenewal` (سطر 110)، دالّة `structuralBreak` (≈334‑349)، وكتلتا تطبيقه (المتوسطة ≈962‑971 والصغيرة ≈1008‑1015)، والبوّابتان `medGate`/`smGate`.
3) اكتب نتيجة تحقّق قصيرة: هل منطق [R2] (كسر شمعتين ⇒ مسح الخط ⇒ انتظار نقطتين جديدتين ⇒ إعادة الرسم) سليم وجاهز للتفعيل؟ إن وجدت أي خلل في المنطق نفسه، أبلغني قبل التفعيل.

# التعديل المطلوب
- غيّر القيمة الافتراضية للمدخل `useRenewal` من `false` إلى `true` (سطر 110)، وأزِل وسم «قيد الاختبار» من عنوانه (يصبح مفعّلاً ومعتمَداً).
- تأكّد أن لا أثر جانبي على التكدّس: خطوط السيولة الآن مقصوصة ضمن النطاق (v1.25)؛ تفعيل التجدّد يمسح الخط عند الكسر وينتظر نقطتين — يجب ألّا يُعيد أي إسقاط غير مقصوص.
- لا تغيّر `structuralBreak` ولا منطق البوّابات (هما مطابقان للدرس) — فقط فعّل المدخل.
- لا تفعّل `useSmallTL` (يبقى مطفأ — تعايُش آليتي قلب الصغيرة لم يُحسم بعد).

# قيود صارمة
- //@version=6، overlay=true، ممنوع lookahead_on.
- لا تكسر البنود المُغلقة: السيولة على الذيول + القصّ (لا تكدّس)، المستوى على الأجسام، [R1][R3][R4][R6]، الصغيرة [A]، المتوسطة [M3][M4][M5].
- لا أرقام/فلاتر جديدة. حافظ على أسماء الـinputs العربية وأسلوب التعليقات.
- التغيير محصور في: القيمة الافتراضية لـ`useRenewal` (وعنوانه). لا تتجاوز ذلك.

# اختبار الاستقرار (مهم — لأن useRenewal كان «قيد الاختبار»)
بعد التفعيل، تحقّق ذهنياً/بصرياً من الحالات التالية وأبلغني بأي عدم استقرار:
1) عند كسر خط السيولة بشمعتين: يُمسح الخط فعلاً، ولا يُعاد رسمه إلا بعد تكوّن نقطتين جديدتين (قاعين صعوداً / قمتين هبوطاً).
2) أثناء الانتظار (الخط ممسوح): لا يتقدّم `brkBar` بلا نهاية، ولا تُرسم سيولة بنقاط قديمة قبل الكسر.
3) لا وميض (flicker) متكرّر للخط على الشمعة الأخيرة، ولا خطوط متراكمة بلا حذف.
4) المراحل (ذعر/يأس/مشاركة) ما زالت تعمل (كسر السيولة بنصف الجسم منفصل عن الكسر البنيوي بشمعتين — تأكّد أنهما لا يتعارضان).
إن ظهر عدم استقرار جوهري ⇒ أرجِع الافتراضي إلى `false` وأبلغني بالسبب بدل فرض تفعيل غير مستقر.

# المخرجات
- diff مختصر (سطر المدخل قبل/بعد).
- ملخّص نتيجة اختبار الاستقرار (النقاط 1‑4).
- كيفية الفحص البصري على TradingView: ارسم موجة، اكسر خط السيولة بشمعتين، وراقب المسح والانتظار وإعادة الرسم بنقطتين.
- ارفع الإصدار v1.26، ثم توقّف للمراجعة.

ابدأ بكتابة نتيجة التحقّق من منطق [R2] قبل التفعيل.
