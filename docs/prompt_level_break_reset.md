اقرأ أولاً ملف scenarios_truth_small.md بالكامل (مرجع الحقيقة الرقميّ). ثم نفّذ هذا الإصلاح.

أنت مهندس M-Waves — Pine v6. فعّل mwaves-engineer. بالعربية. الفرع rebuild-from-scratch، الملف J-Waves-M1.pine (v0.14.0). النطاق: الدروس 1-5 (الصغيرة) فقط — بلا انعكاس اتجاه.

════════ الجذر المؤكَّد (مُثبَت بسيناريوهين رقميّين) ════════
بعد كسر المستوى المحدِّد، لا تُصفَّر مراجع التغطية (lastEffTopPrice/lastEffBottomPrice):
- هبوطاً: lastEffBottomPrice عميق جداً ⇒ قيعان الموجة الجديدة لا تنزل تحته ⇒ القمم لا تتأكّد ⇒ كثير لا يُرسم.
- صعوداً: lastEffTopPrice مرتفع جداً ⇒ قمم الموجة الجديدة لا تتجاوزه ⇒ النقطة الأولى بعد الكسر لا تُرسم.

════════ القاعدة المحسومة (المستخدم) ════════
1. الكسر هبوطاً = السعر (ذيل high) يتجاوز آخر قمّة فعّالة (lastEffTopPrice) — تجاوز صارم ولو بنقطة.
2. الكسر صعوداً = السعر (ذيل low) ينزل تحت آخر قاع فعّال (lastEffBottomPrice) — صارم ولو بنقطة.
3. عند الكسر: صفّر **كلا** المرجعين (lastEffTopPrice := na · lastEffBottomPrice := na). بلا انعكاس اتجاه.
4. النقطة الكاسرة تُرسم بشكل طبيعي (لا استبعاد).
5. لا يُمسّ: الزجزاج (الكشف)، الاتجاه (waveGeneralDir)، المعلّق/التتابُع، آلية feedPivot الداخلية.

════════ المطلوب (تغيير واحد) ════════
أضِف فحص كسر المستوى per-candle في كتلة الكشف (حيث barstate.isconfirmed)، قبل أو بعد منطق الزجزاج، يقرأ مراجع smallDegree:
- هبوطاً (waveGeneralDir == -1): if not na(smallDegree.lastEffTopPrice) and high > smallDegree.lastEffTopPrice ⇒ smallDegree.lastEffTopPrice := na ، smallDegree.lastEffBottomPrice := na
- صعوداً (waveGeneralDir == 1): if not na(smallDegree.lastEffBottomPrice) and low < smallDegree.lastEffBottomPrice ⇒ صفّر الاثنين.
- بالذيل (high/low)، تجاوز صارم (> / <). كور بلا input.
- يُفضَّل وضعه في method على DegreeLaws (مثل checkLevelBreak(self, h, l, waveDir)) للنظافة، يُنادى per-candle.

لا تمسّ الزجزاج ولا الاتجاه ولا التتابُع. تصفير المراجع فقط.

════════ قيود ════════
- التزم MWaves_coding_charter.md. Pine v6 أمر/سطر بلا فاصلة منقوطة. استبدال/إضافة جراحيّة.
- زِد الإصدار v0.14.0 ⇒ v0.15.0 وعلّق «[درس05] كسر المستوى المحدِّد (آخر قمّة/قاع فعّال، بالذيل) ⇒ تصفير مراجع التغطية فيتجدّد التتابُع — بلا انعكاس (نطاق 1-5)».

════════ معيار القبول الرقميّ (من scenarios_truth_small.md — اختبره نقطةً نقطة) ════════
الهبوط: بعد كسر القمّة الفعّالة الأخيرة ⇒ يجب أن تُرسم النقاط 12 (4487.955) · 13 (4537.557) · 18 (4489.039) · 21-23 (4539.864/4426.817/4517.013) — التي كانت تُسقَط. والنقطة 24 (4431.726، ليست أقل من 4426.817) تبقى **غير مرسومة**. والمعلّقة 26 (4365.162) تُفعَّل حين ينزل السعر تحت 4270.703 (يصل 4029).
الصعود: بعد كسر القاع الفعّال الأخير (#9، 51416.01 تحت 51944.66) ⇒ يجب أن تُرسم النقطة 10 (51973.05) التي كانت تُسقَط · 11-16 تبقى مرسومة.
اكتب سطرين «المتوقّع بصرياً» يطابقان هذين.

════════ سؤال مشروع ════════
إن وجدت أن قراءة smallDegree.lastEffTopPrice من كتلة الكشف تحتاج ترتيب تعريف (الـtype معرّف قبل الكتلة؟)، أو أن تصفير المرجعين معاً يكسر النقطة الكاسرة نفسها (يجب أن تُرسم)، أوقِف وبلّغ بمثال. لا تخمّن.

نفّذ، diff + المتوقّع بصرياً، وتوقّف.
