# M-Waves — خطّة الإغلاق (تتبّع تنفيذ الفجوات)
**يكمّل:** `MWaves_deferred_gaps.md` (تفاصيل الفجوات) + `MWaves_lesson08_09_spec.md` (مواصفة الكسور). آخر إصدار: **v3.93.2**.

## المبدأ الحاكم (مُصحَّح)
القمم/القيعان تُبنى أوّلًا مستقلّةً عن الخطوط. **اتجاه استخراج كل درجة = كسر بنيتها الخاصّة** (نمط `*ExtrDir` معزول، مشغَّل بالدرجة الأصغر، **قبل** الاستخراج)، لا الخطوط. الخطوط (سيولة/مستوى) تُبنى على القمم/القيعان وتُغذّي الاستنتاجات (L10)، لا الاستخراج.

## الحالة (بالترتيب)
| البند | الوصف | الحالة |
|---|---|---|
| L05 الصغيرة | أوّل لا أقصى + الزوج + الانقلاب البنيويّ | ✅ كور |
| L06 المتوسطة — اختيار النقطة | `feedMedPair` (أوّل لا أقصى + الزوج) | ✅ كور v3.92.9 |
| L08 الخطوط | سيولة (ذيول) + مستوى (أجسام) ×3 | ✅ مرسومة سلفًا |
| اتجاه السيولة (عرض) | `f_lineDir` + لوحة الاستنتاجات | ✅ v3.93.1 |
| **[D1] اتجاه استخراج المتوسطة + الكبيرة** | = **`bigDir`** (الموجة التاريخية الثابت) حتى التجديد — **لا اتجاه انتقاليّ** (فشل ×4) | ✅ **v3.93.4** |
| [D1‑تمهيد] نقطة بداية الموجة | مربوطة بصحّة `bigDir` (اتجاه الموجة التاريخية) | ⏸ |
| [D2] انقلاب الاتجاه عند انعكاس الموجة التاريخية | يُعالَج في التجديد (الدرس 09) | ⏸ |
| **[07‑ج] اتجاه استخراج الكبيرة** | `bigExtrDir` نفس النمط | ⏭ **التالي** |
| [07‑أ] ATR الكبيرة | إزالة المرشّح الأجنبيّ (`bigMinATR`) | ⏸ |
| [D2] الكسر/الانقلاب/النقل (L09) | كسر المستوى بشمعتين + نقل + قياد | ⏸ |
| [D3] التفعيل اللحظيّ | `calc_on_every_tick` | ⏸ |
| L10 الاستنتاجات من المستوى | الدخول المتاح | ⏸ |
| [05‑أ] فعالية الصغيرة | قرار المستخدم | ⏸ |

## النمط القابل للتكرار `*ExtrDir`
لكلّ درجة D فوق درجة أصغر d:
1. `var int DExtrDir = 0`.
2. **قبل** استخراج D (داخل `if cfHas` نظيرتها): نقطة d فعّالة تكسر بنية D بالجسم ⇒ اقلب `DExtrDir`:
   - قمّة d جسمها > `memD.ltP` ⇒ `+1` · قاع d جسمه < `memD.lbP` ⇒ `-1` · تمهيد `largeDir?:bigDir` عند الصفر.
3. استخراج D يقرأ `DExtrDir` فقط (`wdir`). **آثار D الجانبيّة (الإرساء/مسح الخطوط/قياد الأصغر/L09-10) بلا مساس.**

## معايير القبول (الذهب 1H) — [D1]
- تختفي النقاط المتوسطة الزائفة: 29301.47 (صعود)، 25713.11، 24536.68 (هبوط).
- تعود المفقودة: 25389.34 («قبله»).
- الصغيرة و`medActiveDir` وآثاره بلا تغيير.
